services:
  redis:
    image: redis:7-alpine
    container_name: leon-redis
    ports:
      # Host 6380 → container 6379 (avoids colliding with other local redis).
      - "6380:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    restart: unless-stopped
